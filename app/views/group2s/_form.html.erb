<%= form_for(@group2) do |f| %>
  <% if @group2.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@group2.errors.count, "error") %> prohibited this group2 from being saved:</h2>

      <ul>
      <% @group2.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :organization_date %><br>
    <%= f.datetime_select :organization_date %>
  </div>
  <div class="field">
    <%= f.label :group_id_id %><br>
    <%= f.text_field :group_id_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
